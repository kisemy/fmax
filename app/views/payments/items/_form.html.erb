<%= form_with(model: item) do |form| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
        <% item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


<div class="container-fluid page__container">
  <div class="card card-form">
    <div class="row no-gutters">
      <div class="col-lg-1 card-body">
       
      </div>
      <div class="col-lg-12 card-form__body card-body">
        <div class="row">
          <div class="col">
            <div class="form-group">


  <div class="field">
 <label for="Description"> Description</label>
    <%= form.text_area :description, {class: 'form-control'} %>
  </div>

  <div class="field">
 <label for="unitsofmeasure">Units of Measure</label>

    <%= form.text_area :units_of_measure, {class: 'form-control'} %>
  </div>

  <div class="field">
 <label for="unitcost">Unit Cost</label>
    <%= form.text_field :unit_cost, {class: 'form-control'} %>
  </div>

  <div class="field">
 <label for="unitprice">Unit Price</label>
    <%= form.text_field :unit_price , {class: 'form-control'}%>
  </div>

  <div class="field">
     <label for="inventory">Inventory</label>
    <%= form.text_field :inventory, {class: 'form-control'} %>
  </div>

  <div class="field">
     <label for="shelfno">Shelf No</label>
    <%= form.text_area :shelf_no, {class: 'form-control'} %>
  </div>

  <div class="field">
   <label for="itemcategory">Item Category</label>
    <%= form.text_area :item_category , {class: 'form-control'}%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>


      </div>
    </div>
  </div>
  <div class="text-right mb-5">
    <a href="" class="btn btn-success">Save</a>
  </div>
</div>
